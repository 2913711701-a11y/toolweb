/**
 * 站点配置 —— 全站唯一配置入口。
 *
 * 改这里 → 执行 `node build.js` → 静态页重新生成。
 * 除了「主题色 / 圆角 / 内容宽度」会被写进 CSS 变量，其余都是直接渲染进 HTML。
 *
 * 说明：/tools/ 与 /sites/ 两个路径前缀由构建脚本固定生成，不在此处配置。
 *      如需部署到子目录，只改 site.url（例如 https://example.com/site/），
 *      构建脚本会自动把子路径写进所有链接。
 */
module.exports = {
	/* ---------------------------------------------------------------
	 * 1. 基础信息
	 * ------------------------------------------------------------ */
	site: {
		name: "工具站",                              // 站点名（浏览器标题、页头品牌）
		tagline: "在线工具箱 & 网址导航",             // 品牌下方的副标题，留空则不显示
		description: "精选常用开发与设计工具，全部在浏览器本地运行，无需注册、不上传数据。",
		url: "https://example.com",                  // 部署后的正式域名，用于 canonical / OG / sitemap（末尾不要带斜杠）
		lang: "zh-CN",
		brandInitial: "工",                          // 品牌图标里的字符，留空则取 name 首字
	},

	/* ---------------------------------------------------------------
	 * 2. 首页头部（Hero）
	 * ------------------------------------------------------------ */
	hero: {
		title: "一站式在线工具箱",
		subtitle: "精选常用开发与设计工具，全部本地运行、即开即用，无需注册、不上传数据。",
		placeholder: "搜索工具、网址，例如「JSON」「时间戳」…",
		showSearch: true,
		// 每行一个，点击标签直接触发搜索
		tags: ["JSON 格式化", "时间戳转换", "Base64", "密码生成器", "进制转换"],
	},

	/* ---------------------------------------------------------------
	 * 3. 首页各区块开关与数量
	 * ------------------------------------------------------------ */
	home: {
		showFeaturedTools: true,   // 推荐工具区块
		featuredToolsCount: 8,
		showCategorySections: true, // 按分类浏览
		categorySectionCount: 3,    // 展示前几个分类
		categorySectionTools: 6,    // 每个分类展示几款
		showHomeSites: true,        // 精选网址区块
		homeSitesCount: 12,
	},

	/* ---------------------------------------------------------------
	 * 4. 外观
	 * ------------------------------------------------------------ */
	appearance: {
		primaryColor: "#2563eb",   // 主题色，深色模式下会自动提亮
		radius: 14,                // 圆角基准值（px）
		containerWidth: 1200,      // 内容最大宽度（px），有效范围 960 ~ 1600
		defaultMode: "auto",       // 默认配色：auto 跟随系统 / light 始终浅色 / dark 始终深色
	},

	/* ---------------------------------------------------------------
	 * 5. 页脚（只保留底部的版权 / 备案 / 说明一行）
	 * ------------------------------------------------------------ */
	footer: {
		copyright: "",             // 留空自动生成「© 年份 站点名」
		icp: "",                   // 备案号，例如「京ICP备00000000号」，留空则不显示
		icpHref: "https://beian.miit.gov.cn/",
	},

	/* ---------------------------------------------------------------
	 * 6. 工具详情页的「下载」按钮
	 *
	 * 有链接才渲染按钮 —— 两处都没填链接时，按钮不会出现（避免死按钮）。
	 * 两种配法可混用：
	 *   1) 这里填一个模板，所有工具页共用同一套规则
	 *   2) 在 content/tools.js 的某个工具里单独写 download，覆盖这里的设置
	 * ------------------------------------------------------------ */
	toolDownload: {
		enabled: true,             // 总开关，false 则所有工具页都不显示下载按钮
		label: "下载工具",          // 按钮文字
		url: "",                   // ★ 填这里才会出现按钮。支持占位符 {slug} {name} {category}
		                           //   例："/downloads/{slug}.zip"、"https://pan.example.com/toolbox"
		note: "",                  // 鼠标悬停提示文字，留空则不输出
		newTab: false,             // 是否新标签页打开（外链建议改 true）
		saveAs: false,             // 是否加 HTML download 属性强制另存（仅同源链接有效）
	},

	/* ---------------------------------------------------------------
	 * 7. SEO
	 * ------------------------------------------------------------ */
	seo: {
		enabled: true,             // 总开关
		twitter: true,             // 输出 twitter:card 等标签
		jsonLd: true,              // 输出 JSON-LD 结构化数据
		ogImage: "",               // 分享图 URL，留空则只输出 summary 卡片
	},

	/* ---------------------------------------------------------------
	 * 8. 自定义代码（统计脚本、站长验证等）
	 * ------------------------------------------------------------ */
	code: {
		head: "",                  // 插入到 </head> 前
		footer: "",                // 插入到 </body> 前
	},
};
